## 1.try catch（js）
在script中需要写`try` `catch`吗 ??

## 2.catalog item var中的正则
新建正则<br>
`すべて` > `サービスカタログ` > `カタログ変数` > `変数の妥当性確認の正規表現`

## 3.ATF自动化测试
步骤：<br>
1. 新建用户
2. 模仿1中用户
3. 打开item（SP）
4. 入力
5. check
6. 提交

## 4.catalog item在platform和portal有一些区别
|  | platform  | Portal |
| --- | --- | --- |
| `onChange(control...`的参数control | 可用 | 不可用 |
| 宽度 | 可用 | 不可用 |
| 某些对象 | ... | ... |
